# Optional: Configure Gist sync via environment variables
# These are baked into the build. Only use with a scoped PAT (gist-only permission).
VITE_GIST_TOKEN=your_github_pat_with_gist_scope
VITE_GIST_ID=your_gist_id_from_url
